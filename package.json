{
  "name": "functional-analyser",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Victor Balbo",
  "license": "ISC",
  "devDependencies": {
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  },
  "dependencies": {
    "@types/node-fetch": "^2.1.2",
    "async-file": "^2.0.2",
    "download-git-repo": "^1.1.0",
    "moment": "^2.22.2",
    "node-fetch": "^2.2.0"
  }
}
